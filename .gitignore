# Ignore all root level directories
/*/

# Except the server2 directory
!/server2/

# Un-ignore root level files (optional, but usually safer to keep them tracked if they are there)
!/.gitignore
!/README.md
!/docker-compose.yml
!/Dockerfile
